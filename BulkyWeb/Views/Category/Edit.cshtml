<!--
    Als je hier een Category model aanmaakt maar er wordt vanuit de view niets doorgegeven wordt
    er hier een leeg Category object aangemaakt met de default values!
-->
@model Category



<form method="post">
    <input asp-for="Id" hidden />
    <div class="border p-3 mt-4">
        <div class="row pb-2">
            <h2>Edit Category</h2>
            <hr />
        </div>

        <div asp-validation-summary="ModelOnly"></div>
        <div class="mb-3 row p-1">
            <label class="p-0">Category Name</label>
            <!--Je kan hier de property Name gebruiken omdat je boven het model Category hebt klaar gezet
                Je moet dan ook het type="Text" niet meer aangeven, dat weet hij automatisch!
            -->
            <input asp-for="Name" type="text" class="form-control" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
        <div class="mb-3 row p-1">
            <!--Tag helpers:
                je kan zelfs bij een label zeggen via asp-for dat het voor een bepaalde property bedoelt is en dan moet
                je de tekst zelfs niet meer schrijven.
                Wil je DisplayOrder met een spatie schrijven, dan moet je data annotations aan het model toevoegen
                De tag helpers zorgen er ook voor dat je op het label kan klikken en naar het juiste
                invulveld gaat, of het juiste type suggereert (bv +/- bij integers aanbieden)
            -->
            <label asp-for="DisplayOrder" class="p-0"></label>
            <input asp-for="DisplayOrder" class="form-control" />
            <span asp-validation-for="DisplayOrder" class="text-danger"></span>
        </div>

        <div class="row">
            <div class="col-6 col-md-3">
                <button type="submit" class="btn btn-primary form-control">Update</button>
            </div>
            <div class="col-6 col-md-3">
                <a asp-controller="Category" asp-action="Index" class="btn border btn-outline-secondary form-control">
                    Back to List
                </a>
            </div>
        </div>


    </div>
</form>
@* Omdat de partial view hier script related is moet je dit in @section Scripts plaatsen. Indien je een niet script gerelateerde 
    partial view wilt consumen mag dit gewoon in de form plaatsen met de tag <partial>. 
    Default location voor elke partial view is de 'Shared' folder, dus hij gaat daar zoeken.
    Eerst wordt de client side validation nu uitgevoerd, enkel de custom validation die we geschreven hebben in de categoryController 
    zal op de server getest worden. 
    *@
@section Scripts {
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}
